const Footer = ({ children }) => {

    // UTILIZANDO CHILDREN
    return (
        <div className="container text-center bg-dark text-light mt-3">
            {children}
        </div>
    )

}

export default Footer;